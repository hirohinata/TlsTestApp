設計
モックとの接続（即値のみ）
既存との疑似接続（即値のみ）
新規との疑似接続（即値のみ）
新規との疑似接続（演算込み）
内部演算（最大のみ）
内部演算（自由以外）
内部演算（すべて）
接続監視（一台）
接続監視（複数台）
プリセット
プリセットリセット
校正系　※要分解
CSV出力（Windows）
CSV出力（Linux）
Excel出力

--------

既存との疑似接続（即値のみ）

新規との疑似接続（即値のみ）
    新規側の実装必要

新規との疑似接続（演算込み）
    新規側に演算が必要、こちらはあまり工数必要なし

内部演算（自由以外）
    ひとつできたら工数差あまりなし

内部演算（自由）
    これだけ重い

接続監視
    一台できたら複数も一緒

プリセット
    リセットも一緒
    新規側の時期次第で先行実装か

プリセット（演算）
    内部向けプリセット

校正系
    新規側への情報展開とか　→　これは別
    演算向けの処理とか　→　こっちだけ

CSV出力（Windows）
    別スレッド立てて、対象データベースを読むだけか

CSV出力（Linux）
    電断無視ならWinと同じ
    電断考慮ならSDカードのファイルシステム（FAT32？）理解が必要か


Excel出力
    技術選定から